<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Quick Add Pallet</title>
  <style>
    body { font-family: system-ui; margin: 24px; max-width: 700px; }
    label { display: block; margin-top: 14px; font-weight: 600; }
    select, input, textarea {
      width: 100%;
      padding: 10px;
      margin-top: 4px;
      border: 1px solid #ddd;
      border-radius: 10px;
    }
    button {
      margin-top: 18px;
      padding: 12px 16px;
      border-radius: 10px;
      border: 1px solid #333;
      background: #111;
      color: #fff;
      font-size: 15px;
    }
    .muted { color: #666; font-size: 13px; }
    .error { background: #fee; border: 1px solid #f99; padding: 10px; border-radius: 10px; margin-bottom: 12px; }
  </style>
</head>
<body>

<h1>Quick Add Pallet</h1>
<p class="muted">Use this to load current inventory into a container or the walk-in.</p>

<% if (msg) { %>
  <div class="error"><%= msg %></div>
<% } %>

<form method="post">

  <label>SKU</label>
  <select name="sku_id" required>
    <% skus.forEach(s => { %>
      <option value="<%= s.id %>"><%= s.name %></option>
    <% }) %>
  </select>

  <label>Lot ID (optional — required only for organic)</label>
  <input name="lot_id" placeholder="Leave blank for non-tracked"/>

  <label>Pallet Config ID (optional)</label>
  <input name="pallet_config_id" placeholder="Optional"/>

  <label>Location (example: C3-R01 or WALKIN)</label>
  <input name="location_code" required placeholder="C3-R01"/>

  <label>Quantity</label>
  <input name="qty_units" type="number" step="0.01" required/>

  <label>Notes</label>
  <textarea name="notes"></textarea>

  <button type="submit">Create Pallet</button>
</form>

<p style="margin-top:20px;">
  <a href="/inventory/map?c=1">Back to Container Map</a>
</p>

</body>
</html>
